# Deployment Configuration
# Copy this file to deploy.conf and fill in your server details
# deploy.conf is gitignored and should not be committed

# Server Connection
SERVER_HOST=luboss95vb.com
SERVER_USER=teddy
SSH_PORT=22
SSH_PASSWORD=c2wr5Hp.6485

# Deployment Paths
DEPLOY_PATH=/test
DEPLOY_DIR=/var/www/luboss-vb
BACKEND_PORT=8002  # Safe: Not in use (production uses socket on port 5000)
FRONTEND_PORT=3000  # Safe: Not in use (production uses static files)

# Domain Configuration
DOMAIN=luboss95vb.com
# Note: Production uses custom SSL certificates, not Let's Encrypt
SSL_CERT_PATH=/etc/ssl/luboss
SSL_CERT_FULLCHAIN=${SSL_CERT_PATH}/certificate.crt
SSL_CERT_PRIVKEY=${SSL_CERT_PATH}/private.key

# Nginx Configuration
# Production Nginx config is at: /etc/nginx/sites-available/lubossvb.com (or similar)
# This deployment adds /test location blocks to the existing server block
NGINX_SITES_AVAILABLE=/etc/nginx/sites-available
NGINX_SITES_ENABLED=/etc/nginx/sites-enabled
NGINX_SITE_NAME=luboss-vb  # This will be included in the main production config

# Git Repository (if different from current)
GIT_REPO_URL=https://github.com/teddynyambe/luboss-vb.git
GIT_BRANCH=main

# Service Names
BACKEND_SERVICE_NAME=luboss-backend
FRONTEND_SERVICE_NAME=luboss-frontend

# Database (for migrations)
# Note: Database credentials should be in app/.env on server
# Production uses PostgreSQL on port 5432 (safe, localhost only)
DATABASE_NAME=village_bank
