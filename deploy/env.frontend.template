# Frontend Environment Variables Template
# Copy this to ui/.env.production on the server
# NEXT_PUBLIC_* variables are embedded at build time
# This file should NOT be committed to git

# Base path for deployment (used by next.config.ts)
# Set to /test for subdirectory deployment, empty for root deployment
NEXT_PUBLIC_BASE_PATH=/test

# API URL (embedded at build time)
# Update this to match your deployment path (/test)
NEXT_PUBLIC_API_URL=https://your-domain.com/test/api
